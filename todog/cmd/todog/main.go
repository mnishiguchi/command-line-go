package main

import (
	"github.com/mnishiguchi/command-line-go/todog/internal/cli"
)

// Replaced with the real version during a typical build
var version = "v-dev"

// Placeholder for the real code
func main() {
	cli.Execute(version)
}
